"""
Image processing helpers for overlays/compositing (stub).
"""

def compose_images(images, mode="overlay"):
    """Placeholder image composer."""
    return {"composed": True, "mode": mode, "sources": images}

