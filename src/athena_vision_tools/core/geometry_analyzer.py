"""
Geometry analysis helpers for diffs, measurements, and topology scans (stub).
"""

def diff_geometry(before, after, options=None):
    """Placeholder geometry diff."""
    return {"before": bool(before), "after": bool(after), "options": options or {}}

